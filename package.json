{
  "name": "choix-narratifs",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "lint:ds": "node design/lint/run.mjs",
    "postinstall": "git config core.hooksPath scripts/hooks",
    "test": "vitest run",
    "test:engine": "cargo test --manifest-path engine/Cargo.toml",
    "gen:types": "node scripts/gen-types.mjs",
    "build:engine": "node scripts/build-engine.mjs",
    "deploy:prod": "pnpm build:engine && pnpm build && node scripts/deploy.mjs"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "ajv": "^8.20.0",
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^25.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
